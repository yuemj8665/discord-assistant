# 테스트 결과 — 2026-05-10 00:15

## 테스트 요약

| 항목 | 결과 |
|------|------|
| 테스트 일시 | 2026-05-10 00:15 KST |
| 테스트 목적 | !mail 커맨드 신규 기능 검증 |
| 전체 결과 | ✅ 정상 동작 확인 |

---

## 테스트 분류

| 번호 | 분류 | 항목 | 결과 |
|------|------|------|------|
| 1 | Gmail OAuth | scripts/gmail_auth.py 실행 → data/gmail_token.json 생성 | ✅ 통과 |
| 2 | 메일 발송 | MailService.send() → ***@naver.com 수신 확인 | ✅ 통과 |
| 3 | 봇 재시작 | launchctl kickstart 후 정상 기동 확인 | ✅ 통과 |
| 4 | 채널 제한 | MAIL_CHANNEL_ID(<WORK_CHANNEL_ID>) 외 채널에서 !mail 차단 | 미테스트 |
| 5 | diff 비교 | git_service.get_diff() 스냅샷 기반 비교 | 미테스트 |

---

## 테스트 상세

### 1. Gmail OAuth 인증
- `venv/bin/python3 scripts/gmail_auth.py` 실행
- 브라우저에서 Google 계정(***@gmail.com) 로그인 및 권한 허용
- `data/gmail_token.json` 생성 확인

**사전 요구사항 (신규)**
- Google Cloud Console에서 Gmail API 활성화 필요
- OAuth 동의 화면 → 테스트 사용자에 ***@gmail.com 등록 필요
- credentials.json이 데스크톱 앱 유형이어야 함

### 2. 테스트 메일 발송
```python
mail.send(
    to="***@naver.com",
    subject="[Discord 비서] 테스트 메일",
    body="안녕하세요, 명재님. Gmail API 연동 테스트 메일입니다."
)
```
- 발신: ***@gmail.com (Gmail API)
- 수신: ***@naver.com ✅ 정상 수신 확인

### 3. 봇 재시작
- `_config` 변수 순서 오류(`UnboundLocalError`) 수정 후 정상 기동
- PID 91377로 실행 중 확인
- 로그: `봇 준비 완료: M-MAC#4503`

---

## 발생 에러 및 조치

| 에러 | 원인 | 조치 |
|------|------|------|
| `HttpError 403` — Gmail API 미활성화 | Google Cloud 프로젝트에서 Gmail API 비활성화 상태 | Google Cloud Console에서 Gmail API 활성화 |
| `403: access_denied` — OAuth 차단 | 테스트 모드 앱에 테스트 사용자 미등록 | OAuth 동의 화면 → 테스트 사용자 추가 |
| `UnboundLocalError: _config` | events.py에서 _config 정의 이전에 mail_service 생성 | mail_service 생성 코드를 _config import 이후로 이동 |
